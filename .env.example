# Frontend (Vite)
HOST=localhost
PORT=5337
NODE_ENV=development

# External email microservice — POST { subject?, text, from? }
# Optional: EMAIL_API_KEY for Authorization: Bearer header
VITE_ASSIST_EMAIL_URL=http://localhost:32636/sendEmail
EMAIL_API_KEY=
